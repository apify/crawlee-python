# Tax RAG Scraper Dependencies
# Install the parent crawlee library first: pip install -e ..

# Core dependencies (inherited from parent crawlee project)
# These are already available when crawlee is installed
# crawlee[beautifulsoup]>=1.2.0
# pydantic>=2.11.0
# pydantic-settings>=2.12.0
# httpx>=0.27.0

# Additional project-specific dependencies
# (Add any extra dependencies your project needs here)

# Development dependencies
pytest>=8.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
