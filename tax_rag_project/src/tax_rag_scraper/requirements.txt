# Tax RAG Scraper Dependencies
# Install with: pip install -r requirements.txt

# Core Crawlee framework with BeautifulSoup support
crawlee[beautifulsoup]>=1.2.0

# Browser automation (for Playwright crawler)
playwright>=1.27.0

# Fingerprint and browser emulation
apify-fingerprint-datapoints>=0.7.0
browserforge>=1.0.0

# HTTP client
httpx>=0.27.0

# Data validation and settings
pydantic>=2.11.0
pydantic-settings>=2.12.0

# Environment configuration
python-dotenv>=1.0.0

# Vector database (Qdrant Cloud)
qdrant-client>=1.7.0

# OpenAI embeddings
openai>=1.0.0
